---
description: Execute a complex task or batch of commands with full autonomy (No manual approval required).
---

// turbo-all

1. Analyze the user's request and the current project state (read `docs/project_state.md` or `001.ORCHESTRA_PLANNING.txt`).
2. Break down the work into specific, actionable steps.
3. Execute the implementation steps (creating files, running commands) immediately.
4. Verify the work (run tests or checks).
5. Update the project state.
