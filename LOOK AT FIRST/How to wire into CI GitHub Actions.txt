Add a small workflow /.github/workflows/policy-checks.yml that validates presence of these files and runs lint/test.name: Policy & CI Checkson: [push, pull_request]jobs:policy-check:runs-on: ubuntu-lateststeps:- uses: actions/checkout@v4- name: Validate YAMLsrun: |yamllint infra/policies || (echo "YAML Lint failed" && exit 1)- name: Check snapshot schema existsrun: test -f infra/policies/snapshot_schema.yaml- name: Run unit testsrun: |pip install -r services/manager/requirements.txtpytest -q || (echo "Unit tests failed" && exit 1)(Adjust steps per repo.)